# Imagez Java Client Library

This library is intended to be used by projects that wish to interact with Imagez server.

## Build

To build inside Docker, run this:

    bin/build

## Deploy

To build and deploy library to **Maven**, run:

    bin/deploy

## Usage

```
new ImagezClientHttp(baseUrl,secretToken);
```
